{% extends "admin/layout.twig" %}
{% block title %}  {{l.coupons.1}}{% endblock %}



{% block content %}

<div id="modal_iconified" class="modal fade">
<div class="modal-dialog">
<div class="modal-content">
<div class="modal-header">
<button type="button" class="close" data-dismiss="modal">×</button>
<h5 class="modal-title"><i class="icon-menu7"></i> &nbsp;{{l.bulk_modal.1}}</h5>
</div>

<div class="modal-body">
<div class="alert alert-info alert-styled-left text-blue-800 content-group">
{{l.bulk_modal.2}}
<button type="button" class="close" data-dismiss="alert">×</button>
</div>
{{l.bulk_modal.4}}
<p>
<br>
</p>
</div>

<div class="modal-footer">
<a href="{{ path_for('coupons.bulkdelete') }}" class="btn btn-link text-danger">
{{l.bulk_modal.6}}</a>
<button class="btn btn-primary" data-dismiss="modal"> {{l.bulk_modal.7}} </button>
</div>
</div>
</div>
</div>
<!-- Main content -->
<div class="content-wrapper">



<!-- Page header -->
<div class="page-header page-header page-header-transparent  " >
   
    <div class="page-header-content">
        <div class="page-title">
            <h1><i class="icon-arrow-right6 position-left"></i> <span class="text-semibold">{{l.coupons.1}} </span></h1>
        </div>

        <div class="heading-elements">
           {% if content is not empty %}
            <a href="{{ path_for('coupons.create') }}" class="btn bg-blue btn-labeled heading-btn"><b>
            <i class="icon-plus3"></i></b> اضافة قسيمة جديدة</a>
            
 <a href="javascript:void(0);" data-toggle="modal" data-target="#modal_iconified" class="btn bg-danger btn-labeled heading-btn"><b><i class="icon-trash"></i></b> حذف كل القسائم الشرائية</a>
                        
                        
		     
		          
		               
	






		                                                       
           {% endif %} 
        </div>
    </div>
    
    

    
    
  
</div>
<!-- /page header -->


<!-- Content area -->
<div class="content">
			        {% include "admin/elements/flash.twig" %}


{% if searchView != true %}
{% if content is empty %}
	
			<div class="empty_state text-center">
			    
			<i class="icon-bag empty_state_icon"></i>
			<br/>
			<br/>
		<h6> 
 
        {{l.coupons.4}}
		    
		</h6>
		<br>
		<a href="{{ path_for('coupons.create') }}" class="btn bg-blue heading-btn btn-xlg">
		
		  {{l.coupons.5}}
		  </a>
		
		
		
			</div>
{% endif %}
{% endif %}



{% if content is not empty %}
	
<div class="panel panel-flat">
        <table class="table datatable-basic">
            <thead>
                <tr>
                    <th><b>عنوان القسيمة</b></th>
                    <th><b>رمز القسيمة</b></th>
                    <th><b>تاريخ الإنشاء</b></th>
                    <th><b>الحالة</b></th>
                    <th><b>عدد مرات الإستخدام</b></th>
                    <th class="text-center"><b>تعديل</b></th>
                </tr>
            </thead>
            <tbody>
                {% for item in content %}
                <tr>
                    <td>
                    {% if item.title is empty %}
                    <a href="{{path_for('coupons.edit', {id : item.id})}}">قسيمة بدون عنوان</a>
                    {% else %}
                    <a href="{{path_for('coupons.edit', {id : item.id})}}">{{ item.title}}</a>
                    {% endif %}
                                                
                    </td>
                    <td>
                    <span class="label label-primary label-rounded">{{item.code}}</span>
                    </td>
                    <td>{{item.created_at.toDateTimeString()}}</td>
                    <td>{{item.statueDisplay()}}</td>
                    <td>{{item.count}}</td>
                    <td class="text-center">
                        <ul class="icons-list">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                                    <i class="icon-menu9"></i>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-right">
                                    <li><a href="{{path_for('coupons.edit', {id : item.id})}}" class=""><i class="icon-pencil7"></i><b>تعديل القسيمة </b></a></li>
                                    <li><a href="{{path_for('coupons.delete', {id : item.id})}}" class="text-danger"><i class="icon-trash"></i><b>حذف القسيمة </b></a></li>
                                     <li>
                        <a href="{{path_for('coupons.clone', {id : item.id})}}" class="text-bg-info">
                  <i class="icon-stack-plus position-left"></i>تكرار القسيمة  
              </a>
                        
                        </li>
                                    
                                </ul>
                            </li>
                        </ul>
                    </td>
                </tr>
                {% endfor  %}
            </tbody>
            <thead>
                <tr>
                 <th><b>عنوان القسيمة</b></th>
                    <th><b>رمز القسيمة</b></th>
                    <th><b>تاريخ الإنشاء</b></th>
                    <th><b>الحالة</b></th>
                    <th><b>عدد مرات الإستخدام</b></th>
                    <th class="text-center"><b>تعديل</b></th>
                </tr>
            </thead>
        </table>
</div>

<center>
{{pagination|raw}}
</center>

</div>
{% endif %}

</div>
{% endblock %}